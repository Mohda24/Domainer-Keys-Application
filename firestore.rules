rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /license_keys/{key} {
      allow read, write: if request.auth != null
        && request.auth.uid == "GTpoJcPMMVaCilFxSOnw0E0TtNG3";
    }
  }
}
